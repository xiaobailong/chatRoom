<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
       http://www.springframework.org/schema/util
       http://www.springframework.org/schema/util/spring-util.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 开启组件扫描 -->
    <context:component-scan base-package="com.ioe"/>

    <!-- 配置文件 -->
    <bean id="configFactoryBean" class="com.ioe.config.client.spring.ConfigFactoryBean"/>
    <bean id="properties" factory-bean="configFactoryBean" factory-method="getProperties"/>
    <bean id="propertyPlaceholderConfigurer"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="propertiesArray">
            <util:list>
                <ref bean="properties"/>
            </util:list>
        </property>
    </bean>


    <dubbo:application name="rpc"/>

    <dubbo:registry protocol="zookeeper" address="10.1.170.77:2181"/>

    <dubbo:protocol name="dubbo" threadpool="cached" threads="500"/>

    <!-- 依赖外部的接口 -->
    <dubbo:reference id="notificationService" protocol="dubbo"
                     interface="com.ioe.notification.client.service.NotificationService" version="2.2.0"
                     timeout="30000" check="true"/>
</beans>